//  ListNode* prev=NULL;
//         ListNode* curr;
//         ListNode* next=NULL;
//         curr=head;
//         if(!curr)return NULL;
//         while(curr){
//             next=curr->next;
//             curr->next=prev;
//             prev=curr;
//             curr=next;
            
//         }
      
//         return prev;